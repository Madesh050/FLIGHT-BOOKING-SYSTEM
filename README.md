✈️ Flight Booking System (Core Java)
📖 Overview

The Flight Booking System is a console-based application built using Core Java.
It allows users to perform all major operations related to flight management — including registering new users, searching flights, booking tickets, viewing booked flights, viewing all available flights, and exiting the system.

This project demonstrates the use of Object-Oriented Programming (OOP) concepts, collections, and file handling in Java.

⚙️ Features
👤 User Management

Register a new user with basic details (name, email, password).

Login with registered credentials.

✈️ Flight Operations

View All Flights — Displays all available flights in the system.

Search Flights — Search by source, destination, or date.

Book Flight — Book available flights after logging in.

View Bookings — See all booked flights for the logged-in user.

Exit System — Gracefully exits the application.

🧠 Technologies Used

Programming Language: Java (Core Java)

Concepts Used:

Object-Oriented Programming (Encapsulation, Inheritance, Polymorphism)

Exception Handling

Collections (ArrayList, HashMap)

File Handling (for data storage)

IDE Used: Eclipse / IntelliJ IDEA / VS Code (any Java IDE)
